# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in
this repository.

See `AGENTS.md` — it is the canonical guidance for this repo (what Docer is, the stack
and layout, the architectural consequences of running as a GitHub Actions agent with no
server, and the build/test commands). Kept in one file so the two cannot drift apart.
